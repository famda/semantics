# Standalone devcontainer compose for worker-video
# Uses the production Dockerfile but only builds this single service

x-cuda-support: &cuda-support
  deploy:
    resources:
      reservations:
        devices:
          - driver: nvidia
            count: all
            capabilities: [gpu, utility, compute, video]

services:
  worker-video:
    build:
      context: ../..
      dockerfile: src/Dockerfile
      args:
        WORKER_TYPES: "video"
    tty: true
    stdin_open: true
    environment:
      - TF_ENABLE_ONEDNN_OPTS=0
      - TF_DISABLE_XLA=1
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility,video
      - PYTHONDONTWRITEBYTECODE=1
    volumes:
      - ../../.data/platform/workspaces:/workspaces
      - ../../src/semantics/video/modules:/platform/video/modules:cached
      - ../../src/semantics/video/main.py:/platform/video/main.py:cached
      - ../../src/semantics/video/config.py:/platform/video/config.py:cached
      - ../../src/semantics/global_helpers.py:/platform/video/global_helpers.py:cached
    <<: *cuda-support
